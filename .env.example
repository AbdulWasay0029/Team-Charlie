# VITE_API_BASE_URL: Set this to the live backend server URL (e.g. Render server).
# If left empty or undefined, Bharat Patrol runs in self-contained Mock mode for frontend testing.

VITE_API_BASE_URL=
